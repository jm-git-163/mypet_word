# ── 비밀 열쇠 (절대 올리지 마세요) ──────────────────
.env
.env.local
.env.*.local
*.key
secrets.json

# ── 만들어지는 자료 ────────────────────────────
# 배경 그림(images/bg)은 배포된 앱에 꼭 필요하므로 저장소에 넣습니다.
# 픽사베이 라이선스로 재배포가 가능하고, 전부 합쳐 2.3MB 라 부담이 없습니다.
audio/voice/

# ── 기타 ────────────────────────────────────────
node_modules/
.DS_Store
Thumbs.db
*.log
.vercel
